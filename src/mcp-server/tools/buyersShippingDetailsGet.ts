/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { buyersShippingDetailsGet } from "../../funcs/buyersShippingDetailsGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  buyerId: z.string(),
  shippingDetailsId: z.string(),
  applicationName: z.string().default("core-api"),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$buyersShippingDetailsGet: ToolDefinition<typeof args> = {
  name: "buyers-shipping-details-get",
  description: `Get buyer shipping details

Get a buyer's shipping details.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await buyersShippingDetailsGet(
      client,
      args.buyerId,
      args.shippingDetailsId,
      args.applicationName,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
