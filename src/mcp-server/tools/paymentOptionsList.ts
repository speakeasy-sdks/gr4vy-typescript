/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentOptionsList } from "../../funcs/paymentOptionsList.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.PaymentOptionRequest$inboundSchema.optional(),
};

export const tool$paymentOptionsList: ToolDefinition<typeof args> = {
  name: "payment-options-list",
  description: `List payment options

List the payment options available at checkout. filtering by country, currency, and additional fields passed to Flow rules.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentOptionsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
