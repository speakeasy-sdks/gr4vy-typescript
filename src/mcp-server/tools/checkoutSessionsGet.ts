/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { checkoutSessionsGet } from "../../funcs/checkoutSessionsGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  sessionId: z.string(),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$checkoutSessionsGet: ToolDefinition<typeof args> = {
  name: "checkout-sessions-get",
  description: `Get checkout session

Retrieve the information stored on a checkout session.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutSessionsGet(
      client,
      args.sessionId,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
