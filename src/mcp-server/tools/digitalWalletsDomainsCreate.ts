/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { digitalWalletsDomainsCreate } from "../../funcs/digitalWalletsDomainsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  digitalWalletDomain: components.DigitalWalletDomain$inboundSchema,
  digitalWalletId: z.string(),
  timeoutInSeconds: z.number().default(1),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$digitalWalletsDomainsCreate: ToolDefinition<typeof args> = {
  name: "digital-wallets-domains-create",
  description: `Register a digital wallet domain

Register a digital wallet domain (Apple Pay only).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await digitalWalletsDomainsCreate(
      client,
      args.digitalWalletDomain,
      args.digitalWalletId,
      args.timeoutInSeconds,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
