/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { digitalWalletsSessionsClickToPay } from "../../funcs/digitalWalletsSessionsClickToPay.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  clickToPaySessionRequest: components.ClickToPaySessionRequest$inboundSchema,
  applicationName: z.string().default("core-api"),
};

export const tool$digitalWalletsSessionsClickToPay: ToolDefinition<
  typeof args
> = {
  name: "digital-wallets-sessions-click-to-pay",
  description: `Create a Click to Pay session

Create a session for use with Click to Pay.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await digitalWalletsSessionsClickToPay(
      client,
      args.clickToPaySessionRequest,
      args.applicationName,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
