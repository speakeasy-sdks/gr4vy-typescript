/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { paymentServicesGet } from "../../funcs/paymentServicesGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  paymentServiceId: z.string(),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$paymentServicesGet: ToolDefinition<typeof args> = {
  name: "payment-services-get",
  description: `Get payment service

Get the details of a configured payment service.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentServicesGet(
      client,
      args.paymentServiceId,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
