/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { accountUpdaterJobsCreate } from "../../funcs/accountUpdaterJobsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  accountUpdaterJobCreate: components.AccountUpdaterJobCreate$inboundSchema,
  timeoutInSeconds: z.number().default(1),
};

export const tool$accountUpdaterJobsCreate: ToolDefinition<typeof args> = {
  name: "account-updater-jobs-create",
  description: `Create account updater job

Schedule one or more stored cards for an account update.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountUpdaterJobsCreate(
      client,
      args.accountUpdaterJobCreate,
      args.timeoutInSeconds,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
