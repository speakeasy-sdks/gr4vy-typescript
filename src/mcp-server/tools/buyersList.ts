/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { buyersList } from "../../funcs/buyersList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListBuyersRequest$inboundSchema.optional(),
};

export const tool$buyersList: ToolDefinition<typeof args> = {
  name: "buyers-list",
  description: `List all buyers

List all buyers or search for a specific buyer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await buyersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
