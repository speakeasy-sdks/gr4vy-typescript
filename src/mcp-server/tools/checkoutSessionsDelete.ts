/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { checkoutSessionsDelete } from "../../funcs/checkoutSessionsDelete.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  sessionId: z.string(),
  timeoutInSeconds: z.number().default(1),
  xGr4vyMerchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$checkoutSessionsDelete: ToolDefinition<typeof args> = {
  name: "checkout-sessions-delete",
  description: `Delete checkout session

Deleta a checkout session and all of its (PCI) data.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutSessionsDelete(
      client,
      args.sessionId,
      args.timeoutInSeconds,
      args.xGr4vyMerchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
