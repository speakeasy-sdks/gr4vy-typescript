/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { buyersPaymentMethodsList } from "../../funcs/buyersPaymentMethodsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListBuyerPaymentMethodsRequest$inboundSchema,
};

export const tool$buyersPaymentMethodsList: ToolDefinition<typeof args> = {
  name: "buyers-payment-methods-list",
  description: `List payment methods for a buyer

List all the stored payment methods for a specific buyer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await buyersPaymentMethodsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
