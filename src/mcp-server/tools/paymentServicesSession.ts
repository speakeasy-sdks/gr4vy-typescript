/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { paymentServicesSession } from "../../funcs/paymentServicesSession.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  requestBody: z.record(z.any()),
  paymentServiceId: z.string(),
  applicationName: z.string().default("core-api"),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$paymentServicesSession: ToolDefinition<typeof args> = {
  name: "payment-services-session",
  description: `Create a session for apayment service definition

Creates a session for a payment service that supports sessions.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentServicesSession(
      client,
      args.requestBody,
      args.paymentServiceId,
      args.applicationName,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
