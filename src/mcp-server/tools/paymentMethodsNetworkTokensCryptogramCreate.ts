/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentMethodsNetworkTokensCryptogramCreate } from "../../funcs/paymentMethodsNetworkTokensCryptogramCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.CreatePaymentMethodNetworkTokenCryptogramRequest$inboundSchema,
};

export const tool$paymentMethodsNetworkTokensCryptogramCreate: ToolDefinition<
  typeof args
> = {
  name: "payment-methods-network-tokens-cryptogram-create",
  description: `Provision network token cryptogram

Provision a cryptogram for a network token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentMethodsNetworkTokensCryptogramCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
