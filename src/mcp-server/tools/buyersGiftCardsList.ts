/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { buyersGiftCardsList } from "../../funcs/buyersGiftCardsList.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  buyerExternalIdentifier: z.nullable(z.string()).optional(),
  buyerId: z.nullable(z.string()).optional(),
  timeoutInSeconds: z.number().default(1),
  xGr4vyMerchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$buyersGiftCardsList: ToolDefinition<typeof args> = {
  name: "buyers-gift-cards-list",
  description: `List gift cards for a buyer

List all the stored gift cards for a specific buyer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await buyersGiftCardsList(
      client,
      args.buyerExternalIdentifier,
      args.buyerId,
      args.timeoutInSeconds,
      args.xGr4vyMerchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
