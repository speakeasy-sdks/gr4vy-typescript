/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cardSchemeDefinitionsList } from "../../funcs/cardSchemeDefinitionsList.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$cardSchemeDefinitionsList: ToolDefinition = {
  name: "card-scheme-definitions-list",
  description: `List card scheme definitions

Fetch a list of the definitions of each card scheme.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await cardSchemeDefinitionsList(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
