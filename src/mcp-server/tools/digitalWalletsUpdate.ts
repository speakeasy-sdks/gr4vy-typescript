/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { digitalWalletsUpdate } from "../../funcs/digitalWalletsUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  digitalWalletUpdate: components.DigitalWalletUpdate$inboundSchema,
  digitalWalletId: z.string(),
  timeoutInSeconds: z.number().default(1),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$digitalWalletsUpdate: ToolDefinition<typeof args> = {
  name: "digital-wallets-update",
  description: `Update digital wallet

Update a digital wallet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await digitalWalletsUpdate(
      client,
      args.digitalWalletUpdate,
      args.digitalWalletId,
      args.timeoutInSeconds,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
