/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { buyersShippingDetailsDelete } from "../../funcs/buyersShippingDetailsDelete.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  buyerId: z.string(),
  shippingDetailsId: z.string(),
  timeoutInSeconds: z.number().default(1),
};

export const tool$buyersShippingDetailsDelete: ToolDefinition<typeof args> = {
  name: "buyers-shipping-details-delete",
  description: `Delete a buyer's shipping details

Delete the shipping details associated to a specific buyer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await buyersShippingDetailsDelete(
      client,
      args.buyerId,
      args.shippingDetailsId,
      args.timeoutInSeconds,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
