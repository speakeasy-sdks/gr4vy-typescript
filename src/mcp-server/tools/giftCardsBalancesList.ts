/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { giftCardsBalancesList } from "../../funcs/giftCardsBalancesList.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  giftCardBalanceRequest: components.GiftCardBalanceRequest$inboundSchema,
  timeoutInSeconds: z.number().default(1),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$giftCardsBalancesList: ToolDefinition<typeof args> = {
  name: "gift-cards-balances-list",
  description: `List gift card balances

Fetch the balances for one or more gift cards.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await giftCardsBalancesList(
      client,
      args.giftCardBalanceRequest,
      args.timeoutInSeconds,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
