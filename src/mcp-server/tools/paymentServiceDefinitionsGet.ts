/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { paymentServiceDefinitionsGet } from "../../funcs/paymentServiceDefinitionsGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  paymentServiceDefinitionId: z.string(),
  applicationName: z.string().default("core-api"),
};

export const tool$paymentServiceDefinitionsGet: ToolDefinition<typeof args> = {
  name: "payment-service-definitions-get",
  description: `Get a payment service definition

Get the definition of a payment service that can be configured.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentServiceDefinitionsGet(
      client,
      args.paymentServiceDefinitionId,
      args.applicationName,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
