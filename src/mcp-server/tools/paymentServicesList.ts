/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentServicesList } from "../../funcs/paymentServicesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPaymentServicesRequest$inboundSchema.optional(),
};

export const tool$paymentServicesList: ToolDefinition<typeof args> = {
  name: "payment-services-list",
  description: `List payment services

List the configured payment services.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentServicesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
