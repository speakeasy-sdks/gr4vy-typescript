/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { buyersShippingDetailsUpdate } from "../../funcs/buyersShippingDetailsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateBuyerShippingDetailsRequest$inboundSchema,
};

export const tool$buyersShippingDetailsUpdate: ToolDefinition<typeof args> = {
  name: "buyers-shipping-details-update",
  description: `Update a buyer's shipping details

Update the shipping details associated to a specific buyer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await buyersShippingDetailsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
