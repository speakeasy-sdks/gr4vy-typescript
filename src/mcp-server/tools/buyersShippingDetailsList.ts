/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { buyersShippingDetailsList } from "../../funcs/buyersShippingDetailsList.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  buyerId: z.string(),
  applicationName: z.string().default("core-api"),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$buyersShippingDetailsList: ToolDefinition<typeof args> = {
  name: "buyers-shipping-details-list",
  description: `List a buyer's shipping details

List all the shipping details associated to a specific buyer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await buyersShippingDetailsList(
      client,
      args.buyerId,
      args.applicationName,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
