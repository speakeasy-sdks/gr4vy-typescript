/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { transactionsEventsList } from "../../funcs/transactionsEventsList.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  transactionId: z.string(),
  cursor: z.nullable(z.string()).optional(),
  limit: z.number().int().default(100),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$transactionsEventsList: ToolDefinition<typeof args> = {
  name: "transactions-events-list",
  description: `List transaction events

Fetch a list of events for a transaction.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsEventsList(
      client,
      args.transactionId,
      args.cursor,
      args.limit,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
