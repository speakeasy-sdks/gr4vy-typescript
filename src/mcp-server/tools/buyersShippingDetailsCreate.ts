/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { buyersShippingDetailsCreate } from "../../funcs/buyersShippingDetailsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  shippingDetailsCreate: components.ShippingDetailsCreate$inboundSchema,
  buyerId: z.string(),
  applicationName: z.string().default("core-api"),
  merchantAccountId: z.nullable(z.string()).optional(),
};

export const tool$buyersShippingDetailsCreate: ToolDefinition<typeof args> = {
  name: "buyers-shipping-details-create",
  description: `Add buyer shipping details

Associate shipping details to a buyer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await buyersShippingDetailsCreate(
      client,
      args.shippingDetailsCreate,
      args.buyerId,
      args.applicationName,
      args.merchantAccountId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
