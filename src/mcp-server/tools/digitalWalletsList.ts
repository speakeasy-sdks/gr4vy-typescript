/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { digitalWalletsList } from "../../funcs/digitalWalletsList.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$digitalWalletsList: ToolDefinition = {
  name: "digital-wallets-list",
  description: `List digital wallets

List configured digital wallets.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await digitalWalletsList(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
