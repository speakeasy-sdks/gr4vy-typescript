/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import {
    GiftCard,
    GiftCard$inboundSchema,
    GiftCard$Outbound,
    GiftCard$outboundSchema,
} from "./giftcard.js";
import * as z from "zod";

/**
 * A list of stored gift cards.
 */
export type GiftCards = {
    /**
     * A list of stored gift cards.
     */
    items?: Array<GiftCard> | undefined;
    /**
     * The limit applied to request. This represents the number of items that are at
     *
     * @remarks
     * maximum returned by this request.
     */
    limit?: number | undefined;
    /**
     * The cursor that represents the next page of results. Use the `cursor` query
     *
     * @remarks
     * parameter to fetch this page of items.
     */
    nextCursor?: string | null | undefined;
    /**
     * The cursor that represents the next page of results. Use the `cursor` query
     *
     * @remarks
     * parameter to fetch this page of items.
     */
    previousCursor?: string | null | undefined;
};

/** @internal */
export const GiftCards$inboundSchema: z.ZodType<GiftCards, z.ZodTypeDef, unknown> = z
    .object({
        items: z.array(GiftCard$inboundSchema).optional(),
        limit: z.number().int().default(20),
        next_cursor: z.nullable(z.string()).optional(),
        previous_cursor: z.nullable(z.string()).optional(),
    })
    .transform((v) => {
        return remap$(v, {
            next_cursor: "nextCursor",
            previous_cursor: "previousCursor",
        });
    });

/** @internal */
export type GiftCards$Outbound = {
    items?: Array<GiftCard$Outbound> | undefined;
    limit: number;
    next_cursor?: string | null | undefined;
    previous_cursor?: string | null | undefined;
};

/** @internal */
export const GiftCards$outboundSchema: z.ZodType<GiftCards$Outbound, z.ZodTypeDef, GiftCards> = z
    .object({
        items: z.array(GiftCard$outboundSchema).optional(),
        limit: z.number().int().default(20),
        nextCursor: z.nullable(z.string()).optional(),
        previousCursor: z.nullable(z.string()).optional(),
    })
    .transform((v) => {
        return remap$(v, {
            nextCursor: "next_cursor",
            previousCursor: "previous_cursor",
        });
    });

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace GiftCards$ {
    /** @deprecated use `GiftCards$inboundSchema` instead. */
    export const inboundSchema = GiftCards$inboundSchema;
    /** @deprecated use `GiftCards$outboundSchema` instead. */
    export const outboundSchema = GiftCards$outboundSchema;
    /** @deprecated use `GiftCards$Outbound` instead. */
    export type Outbound = GiftCards$Outbound;
}
