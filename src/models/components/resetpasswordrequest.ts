/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * A request to reset a password for a user.
 */
export type ResetPasswordRequest = {
    /**
     * The email address of the user account to reset.
     */
    emailAddress?: string | undefined;
};

/** @internal */
export namespace ResetPasswordRequest$ {
    export type Inbound = {
        email_address?: string | undefined;
    };

    export const inboundSchema: z.ZodType<ResetPasswordRequest, z.ZodTypeDef, Inbound> = z
        .object({
            email_address: z.string().optional(),
        })
        .transform((v) => {
            return {
                ...(v.email_address === undefined ? null : { emailAddress: v.email_address }),
            };
        });

    export type Outbound = {
        email_address?: string | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, ResetPasswordRequest> = z
        .object({
            emailAddress: z.string().optional(),
        })
        .transform((v) => {
            return {
                ...(v.emailAddress === undefined ? null : { email_address: v.emailAddress }),
            };
        });
}
